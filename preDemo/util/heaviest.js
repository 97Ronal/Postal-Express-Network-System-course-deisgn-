function heaviest(myweight) {
  var myweight1;
  var myweight2;
  var myzero = 0;
  var myaddress;
  for (var i = 0; i < length.myweight; i++) {
    myweight1[i] = goods[myweight[i]].weight;
  }
  //引用sort.numeric
  var myweight2 = myweight2.sort(sortNumber);
  myaddress = myweight2[myweight2.length - 1];
  return (myaddress)
}